/**
 * This class takes care of enabling the python bot to make moves in the chessapp
 */
var Bot = function(diff){
    that.diff = diff

    function calculateMove(data){
        console.log("Calculating move")
    }
}
